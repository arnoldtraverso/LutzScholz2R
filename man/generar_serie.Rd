% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generacion.R
\name{generar_serie}
\alias{generar_serie}
\title{Generacion de la serie sintetica de caudales (proceso markoviano)}
\usage{
generar_serie(
  pe_matriz,
  calib,
  params = huancane_params(),
  aleatorios = NULL,
  seed = NULL,
  r2 = calib$r2_aj
)
}
\arguments{
\item{pe_matriz}{Matriz ano x mes de precipitacion efectiva (ver \link{pe_serie}).}

\item{calib}{Objeto \link{calibrar_regresion}.}

\item{params}{Objeto \link{lutz_params} (aporta \code{q0}).}

\item{aleatorios}{Matriz ano x mes de numeros aleatorios \code{z}. Si es \code{NULL} se
generan con \code{rnorm}.}

\item{seed}{Semilla para \code{rnorm} cuando \code{aleatorios} es \code{NULL} (reproducibilidad).}

\item{r2}{Coeficiente de determinacion para el termino de ruido. Por defecto el
R^2 ajustado de la calibracion (como en la plantilla original).}
}
\value{
\code{data.frame} ano x mes con los caudales generados (m3/s), mas columna
\code{anio} si la matriz de PE trae anios en \code{rownames}.
}
\description{
Genera caudales medios mensuales mediante el modelo autoregresivo de primer
orden con termino estocastico:
\deqn{Q_t = | b_1 + b_2 Q_{t-1} + b_3 PE_t + z_t \cdot S \cdot \sqrt{1 - R^2} |}
La serie se encadena mes a mes y ano tras ano; el primer mes usa el caudal base
\code{q0} como \code{Qt-1}.
}
